# Copy this file to .env and supply your own credentials locally.
# Never commit real API keys.

OPENAI_API_KEY=

# Optional runtime model override. If unset, a project file may provide its own model.
MGATE_MODEL=
